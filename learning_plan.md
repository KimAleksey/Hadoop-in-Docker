# План изучения Hadoop и Spark 🐘⚡️

Этот план разработан специально для использования в вашем развернутом Docker-окружении. Мы начнем с основ и будем двигаться к более сложным концепциям.

## Модуль 1: Основы HDFS (Hadoop Distributed File System)
**Цель:** Научиться управлять файловой системой кластера.

1.  **Архитектура HDFS**:
    *   NameNode vs DataNode (Посмотрите в UI: http://localhost:9870).
    *   Блоки, репликация (почему файлы разбиваются на части).
2.  **Практика (CLI Команды)**:
    *   `hdfs dfs -ls /` (Просмотр)
    *   `hdfs dfs -mkdir /user/myname` (Создание папок)
    *   `hdfs dfs -put localfile.txt /user/myname/` (Загрузка данных)
    *   `hdfs dfs -cat /user/myname/localfile.txt` (Чтение)
    *   `hdfs dfs -rm /user/myname/localfile.txt` (Удаление)

## Модуль 2: MapReduce и YARN
**Цель:** Понять классическую модель вычислений и управление ресурсами.

1.  **Концепция MapReduce**:
    *   Map (Фильтрация/Сортировка) -> Shuffle -> Reduce (Агрегация).
2.  **YARN (Yet Another Resource Negotiator)**:
    *   ResourceManager (Master) и NodeManager (Worker).
    *   Посмотрите UI: http://localhost:8088.
3.  **Практика**:
    *   Запуск классического примера **WordCount** на Hadoop Streaming (Python) или Java.
    *   Наблюдение за задачей в YARN UI.

## Модуль 3: Spark Core и RDD
**Цель:** Перейти к современным быстрым вычислениям в памяти.

1.  **Архитектура Spark**:
    *   Driver, Master, Worker, Executor.
    *   UI: http://localhost:8090.
2.  **RDD (Resilient Distributed Dataset)**:
    *   Неизменяемые коллекции объектов.
    *   Transformations (map, filter) vs Actions (count, collect).
    *   Lazy evaluation (ленивые вычисления).
3.  **Практика (Spark Shell)**:
    *   Интерактивная работа с данными в `spark-shell`.
    *   Чтение файла из HDFS в RDD.

## Модуль 4: Spark SQL и DataFrames
**Цель:** Работа со структурированными данными (как в Pandas или SQL).

1.  **TDataFrame / Dataset**:
    *   Схожесть с таблицами реляционных БД.
    *   Оптимизатор Catalyst.
2.  **Spark SQL**:
    *   Выполнение SQL-запросов над данными.
    *   Чтение/запись JSON, CSV, Parquet.
3.  **Практика**:
    *   Загрузка CSV в DataFrame.
    *   Агрегация данных (groupBy, count).
    *   Сохранение результата обратно в HDFS.

## Модуль 5: Продвинутые темы (по желанию)
1.  **Hive**: SQL-интерфейс поверх HDFS (можно развернуть дополнительно).
2.  **Spark Streaming**: Обработка данных в реальном времени.

---

### 🚀 Первый шаг: Практика HDFS
Давайте начнем с Модуля 1. Ваше первое задание:
1.  Зайти в контейнер `namenode`.
2.  Создать свою домашнюю директорию в HDFS.
3.  Загрузить туда любой текстовый файл.
4.  Проверить, что файл появился в веб-интерфейсе (http://localhost:9870).

Готовы приступить?
