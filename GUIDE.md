# 🐘 Путеводитель по Экосистеме Hadoop и Spark
*Для тех, кто начинает свой путь в Big Data*

Этот документ создан специально для того, чтобы объяснить "на пальцах", что происходит внутри нашего `docker-compose` проекта, зачем нужны все эти контейнеры, и как с ними работать.

---

## 🏗 Структура Проекта (Архитектура)

Наш "кластер" состоит из множества сервисов (контейнеров), каждый из которых выполняет свою роль. В Big Data принято разделять **хранение данных** и **обработку данных**.

### 1. Слой Хранения: HDFS (Hadoop Distributed File System)
Представьте, что у вас есть файл размером 100 Терабайт. Он не поместится на один жесткий диск. HDFS решает эту проблему, "разрезая" файл на кусочки (блоки) и разбрасывая их по многим компьютерам.

В нашем проекте за это отвечают два сервиса:
*   **NameNode (`namenode`)**: "Мозг" файловой системы. Он не хранит сами данные, он хранит *адреса*. Он знает: "Файл `data.csv` разрезан на 3 блока: блок А лежит на сервере 1, блок Б на сервере 2...".
    *   *Аналогия*: Библиотекарь с каталогом.
*   **DataNode (`datanode`)**: "Рабочая лошадка". Обычный диск, который просто хранит куски байтов и отдает их по команде.
    *   *Аналогия*: Полки с книгами.

**Почему используется?** Надежность и масштабируемость. Если один DataNode сгорит, данные не пропадут (при правильной репликации).

### 2. Слой Управления Ресурсами: YARN
Когда вы хотите запустить программу обработки данных, ей нужны процессор и память. YARN (Yet Another Resource Negotiator) — это "менеджер", который выдает эти ресурсы.

*   **ResourceManager (`resourcemanager`)**: Главный начальник. Принимает заявки: "Мне нужно 10 ядер и 100 Гб памяти".
*   **NodeManager (`nodemanager`)**: Агент на каждом компьютере. Докладывает начальнику: "У меня свободно 4 ядра". Выполняет приказы по запуску задач.

### 3. Слой Обработки: Apache Spark ⚡️
Hadoop MapReduce (старый движок) был медленным, потому что постоянно писал промежуточные результаты на диск. **Spark** делает всё в оперативной памяти (RAM), поэтому он в 100 раз быстрее.

*   **Spark Master (`spark-master`)**: Координатор кластера Spark. Распределяет задачи.
*   **Spark Worker (`spark-worker`)**: Выполняет вычисления.
*   **Spark Thrift Server (`spark-thrift-server`)**: Специальный сервис, который притворяется базой данных. Позволяет подключаться к Spark через SQL-клиенты (DBeaver, Tableau) по протоколу JDBC.

### 4. Слой Метаданных: Hive Metastore 🐝
Spark и HDFS хранят просто *файлы*. Но аналитикам удобно работать с *таблицами* (колонки, типы данных).
**Hive Metastore** — это словарь, который связывает файлы с таблицами.
*   Пример: "Таблица `users` — это CSV файлы, лежащие в папке `/user/hive/warehouse/users`, разделитель запятая".
*   Мы используем **PostgreSQL** (`hive-metastore-postgresql`) как надежную базу для хранения этих определений.

### 5. Инструменты Доступа
*   **Jupyter Notebook (`jupyter`)**: Веб-интерфейс для написания кода на Python (PySpark). Идеально для экспериментов и Data Science.
*   **DBeaver (на вашем ПК)**: SQL-клиент для аналитиков. Подключается к Thrift Server.

---

## 🚀 Примеры Задач (Практика)

Давайте пройдем путь от простого файла до аналитического отчета.

### Задача 1: Работа с "Голой" Файловой Системой (HDFS CLI)
**Цель:** Загрузить файл внутрь кластера.

1.  Откройте терминал в Jupyter (New -> Terminal).
2.  Создайте локальный файл:
    ```bash
    echo "Hello Hadoop" > hello.txt
    ```
3.  Положите его в HDFS:
    ```bash
    # Создать папку
    hdfs dfs -mkdir -p /user/test_data
    # Скопировать файл
    hdfs dfs -put hello.txt /user/test_data/
    # Проверить список файлов
    hdfs dfs -ls /user/test_data/
    # Прочитать содержимое из HDFS
    hdfs dfs -cat /user/test_data/hello.txt
    ```

### Задача 2: Анализ Данных через PySpark (Python)
**Цель:** Прочитать CSV, отфильтровать и сохранить в эффективном формате Parquet.

1.  Откройте Jupyter Notebook.
2.  Код:
    ```python
    from pyspark.sql import SparkSession

    # 1. Создание сессии (точка входа)
    spark = SparkSession.builder.appName("MyFirstJob").enableHiveSupport().getOrCreate()

    # 2. Создание тестовых данных
    data = [("Alice", 25), ("Bob", 30), ("Charlie", 35)]
    df = spark.createDataFrame(data, ["name", "age"])

    # 3. Трансформация (фильтр)
    young_people = df.filter(df.age < 30)

    # 4. Сохранение результата как ТАБЛИЦУ (чтобы видеть в DBeaver)
    # mode("overwrite") перезапишет таблицу, если она есть
    young_people.write.mode("overwrite").saveAsTable("young_employees")
    ```

### Задача 3: SQL Аналитика (DBeaver)
**Цель:** Найти данные, которые мы только что создали в Python.

1.  Подключитесь DBeaver к `localhost:10000`.
2.  Вполните SQL запрос:
    ```sql
    -- Мы увидим таблицу, созданную в Python!
    SELECT * FROM young_employees;
    ```
3.  Создайте новую таблицу через SQL:
    ```sql
    CREATE TABLE salaries (name STRING, salary INT);
    INSERT INTO salaries VALUES ('Alice', 50000), ('Bob', 60000);
    ```

### Задача 4: Объединение (Join) Python и SQL
**Цель:** В Python объединить данные о сотрудниках с их зарплатами.

```python
# Читаем таблицу, созданную через SQL
salaries_df = spark.sql("SELECT * FROM salaries")

# Читаем таблицу сотрудников
employees_df = spark.table("young_employees")

# Джойн
report = employees_df.join(salaries_df, "name")
report.show()
```

---

## 🎓 Зачем всё это нужно?

1.  **Масштаб**: Если данных станет 1 Петабайт, вы просто добавите в `docker-compose` (или в реальный дата-центр) еще 10 контейнеров `datanode`, и система продолжит работать. Excel так не умеет.
2.  **Отказоустойчивость**: Если сервер упадет, данные возьмутся из реплики на другом сервере.
3.  **Универсальность**:
    *   Data Scientist пишет на Python.
    *   Аналитик пишет SQL.
    *   Оба работают с ОДНИМИ И ТЕМИ ЖЕ данными.

Это и есть суть **Data Lake** (Озера Данных). Ваша песочница — это мини-модель того, что работает в банках, телекомах и IT-гигантах. Удачи в изучении! 🚀
